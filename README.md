`dataframe-plot`
====

tktktk

Examples
----

Examples for different high level plots are generated as JSON during test runs.
Outputs from tests are converted to PNGs using `vl-convert` (TODO -- which unfortunately
can't be built with nix for reasons? I don't know what's going on with deno, maybe someone
knows better than I do)
